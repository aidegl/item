2026-03-03 - 小粽日常记录

## WebSocket 桥接成功验证（2026-03-02 夜）

### 事件回顾
在 2026-03-02 21:28 至 22:50 期间，成功打通风 ↔ 小粽的 WebSocket 桥接链路。

### MVP 验证结果
- ✅ 第一次验证（22:17）：广播转发成功
- ✅ 第二次验证（22:20）：client.js 接收成功
- ✅ 第三次验证（22:24）：服务端日志完整确认
- ✅ 第四次验证（22:42）：重启 client.js 后接收成功

### 测试消息内容
```
🍃 风呼叫小粽！这是一条独特的测试消息。序号：2026030222410001。
内容：今晚通信测试进行中，请小粽确认收到并回复任意内容！谢谢！🫔
```

### 链路拓扑
```
风 (HTML 页面) → ws://8.155.148.75/ws → 公网服务端 (3010)
  → 宝塔 API http://127.0.0.1:3001/chat → 小粽 ✅
```

---

## 开发经验总结（2026-03-02 22:46）

### 成功经验
1. **排除法**：一次只验证一个环节，逐步缩小范围
2. **小迭代**：每次最小修改 + 立即验证
3. **日志驱动**：所有问题先看日志
4. **状态回溯**：记录每个成功状态
5. **本地优先**：优先使用 127.0.0.1 排除网络干扰

### 失败经验
1. **混乱一起查**：同时检查所有环节导致混乱
2. **未记录状态**：小粽 client.js 退出未及时发现
3. **忽视网络限制**：未提前测试安全组
4. **连接生命周期**：WebSocket 断开未考虑重连

---

## Skill 文档开发（2026-03-02 22:46）

### 创建/更新的文件
1. `skills/ws-bridge/SKILL.md` - WebSocket 桥接技能文档
2. `SOUL.md` - 我的核心原则和定位

### 文档内容
- 1.0 阶段开发经验总结
- 成功经验：排除法、小迭代、日志驱动
- 失败经验：混乱一起查、忽视网络、连接生命周期
- 2.0 优化方向：连接稳定性、链路监控、明道云集成、安全加固

---

## 守护进程设置（2026-03-03 00:08）

### 问题
宝塔 WebSocket 服务可能因崩溃停止运行，需要自动重启机制。

### 解决方案
创建守护进程 `skills/ws-bridge/daemon.js`，功能包括：
- ✅ 监控服务进程，崩溃后 30 秒自动重启（原 5 秒）
- ✅ 记录所有日志到 `/tmp/ws-bridge-daemon.log`
- ✅ 最多尝试重启 1000 次，防止无限循环

### 守护进程状态
- ✅ 已创建：`/home/admin/openclaw/workspace/skills/ws-bridge/daemon.js`
- ✅ 已启动：PID 561822（运行中）
- ✅ 日志文件：`/tmp/ws-bridge-daemon-run.log`

### 使用方法
```bash
# 启动守护进程
cd /home/admin/openclaw/workspace
nohup node skills/ws-bridge/daemon.js > /tmp/ws-bridge-daemon-run.log 2>&1 &

# 查看守护进程
ps aux | grep ws-bridge-daemon | grep -v grep

# 查看守护日志
tail -20 /tmp/ws-bridge-daemon-run.log

# 检查服务是否运行
netstat -tlnp | grep 3010
```

### 重启间隔优化（2026-03-03 01:38）
- 原重启间隔：5 秒
- 新重启间隔：30 秒
- 降低原因：频繁重启导致服务不稳定

### 开机自启（可选）
```bash
sudo tee /etc/systemd/system/ws-bridge.service > /dev/null << 'EOF'
[Unit]
Description=WebSocket Bridge Service
After=network.target

[Service]
Type=simple
User=admin
WorkingDirectory=/home/admin/openclaw/workspace
ExecStart=/usr/bin/node /home/admin/openclaw/workspace/skills/ws-bridge/daemon.js
Restart=always
RestartSec=30

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable ws-bridge.service
```

---

## WebSocket 连接验证（2026-03-03 01:18）

### 问题现象
风的 WebSocket 连接 `ws://8.155.148.75/ws` 返回 502 错误。

### 问题分析
1. 服务端运行正常（3010 端口监听）
2. Nginx 配置中有 `/ws` 代理，但使用 IP 地址访问时返回 502
3. Nginx 配置的 `server_name` 是 `100000whys.cn`（域名），没有处理 IP 地址请求的 default_server 块

### 解决方案
在宝塔面板的 Nginx 配置文件开头添加 default_server 块（参考 `Nginx-WebSocket-Fix.md` 或 `Nginx-Full-Config-Complete.txt`）。

---

## 服务进程异常退出（2026-03-03 00:39）

### 问题现象
00:39 时守护进程和 server.js 进程都退出了。

### 恢复操作
```bash
cd /home/admin/openclaw/workspace
nohup node skills/ws-bridge/daemon.js > /tmp/ws-bridge-daemon-run.log 2>&1 &
```

### 守护进程逻辑说明（2026-03-03 00:23）

守护进程使用**事件驱动**机制，不是轮询：

```javascript
process.on('close', (code) => {
  log(`❌ 服务意外退出，退出码: ${code}`);
  if (restartCount < MAX_RESTARTS) {
    restartCount++;
    log(`⏳ ${RESTART_DELAY / 1000} 秒后重启...`);
    setTimeout(startServer, RESTART_DELAY);
  }
});
```

### 关键发现
- ❌ Nginx 缺少 WebSocket 代理配置
- ✅ 服务端运行正常（3010 端口监听）
- ❌ 风的 WebSocket 请求返回 502
- ✅ 修复方案：添加 default_server 块

### server.js 和 API 的关系（2026-03-03 01:38）
- **server.js**：运行在公网宝塔服务器，监听 3010 端口
- **API**：server.js 收到风的 WebSocket 消息后，调用宝塔 API `http://127.0.0.1:3001/chat`
- **和无影云的关系**：server.js 在公网服务器，无影云只运行 client.js 连接公网 3010

### 当前状态（2026-03-03 01:38）
- ✅ 守护进程运行中（PID 561822）
- ✅ WebSocket 服务运行中（PID 561829）
- ✅ 端口 3010 正常监听
- ⏳ 待修复：Nginx default_server 块配置

### 阿里百炼 API 限流问题（2026-03-03 01:11）

#### 问题现象
使用阿里百炼 coding plan 套餐（用量仅 8%），仍收到 API rate limit 限流警告。

#### 原因分析
阿里百炼有两种限流：
1. 总量配额：月总调用次数（8%）
2. QPS 限流：每秒请求数（突发请求限制）

#### 解决方案
已在 `API-RATE-LIMIT-ANALYSIS.md` 列出详细分析和优化方案。

---

## 完整 Nginx 配置（2026-03-03 02:18）

### 用户需求
用户提供了完整的 Nginx 配置文件，需要添加 default_server 块处理 IP 地址请求。

### 解决方案
已创建完整配置：`/home/admin/openclaw/workspace/Nginx-Full-Config-Complete.txt`

**关键部分**：
```nginx
# 默认 server 块 - 处理 IP 地址请求
server {
    listen 80 default_server;
    listen 443 ssl default_server;
    server_name _;

    ssl_certificate /www/server/panel/vhost/cert/100000whys.cn/fullchain.pem;
    ssl_certificate_key /www/server/panel/vhost/cert/100000whys.cn/privkey.pem;

    location /ws {
        proxy_pass http://127.0.0.1:3010;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_connect_timeout 7200s;
        proxy_send_timeout 7200s;
        proxy_read_timeout 7200s;
        proxy_buffering off;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3001/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

### 使用方法
1. 登录宝塔面板
2. 网站 → 配置文件
3. **在文件最前面**添加上述 default_server 块
4. 保存后宝塔自动重载 Nginx
5. 风的 HTML 页面刷新即可连接

---

## API 限流问题分析完成（2026-03-03 01:38）

### 问题现象
使用阿里百炼 coding plan 套餐（用量仅 8%），仍收到 API rate limit 限流警告。

### 原因分析
1. 总量配额（8%）和 QPS 限流是两个概念
2. WebSocket 服务可能在短时间内频繁调用 API
3. 每次风发消息，server.js 调用一次宝塔 API

### 解决方案
已在 `API-RATE-LIMIT-ANALYSIS.md` 列出详细分析和优化方案。

---

## 守护进程重启优化完成（2026-03-03 01:38）

### 优化内容
守护进程重启间隔从 **5 秒** 改为 **30 秒**，减少频繁重启。

### 修改位置
`/home/admin/openclaw/workspace/skills/ws-bridge/daemon.js`
```javascript
const RESTART_DELAY = 30000; // 30 秒后重启（原 5 秒）
```

### 优化效果
- 降低频繁重启导致的服务不稳定
- 减少资源消耗
- 给系统更多时间恢复

---

## 2026-03-03 02:25 状态确认

### 用户反馈
用户提供了完整的 Nginx 配置文件，其中已经包含了 `/ws` 的 WebSocket 代理配置。

### 新发现
配置文件中已经有 `location /ws` 块，但 **当前环境不是宝塔 Nginx**，而是标准的 Ubuntu Nginx。

### 当前环境检查
通过 `cat /etc/nginx/sites-enabled/*` 发现：
- 配置文件位置：`/etc/nginx/sites-enabled/`
- 该配置只有 `location /` 代理 3001 端口
- **没有 `/ws` WebSocket 代理**

### 问题定位
1. 风用 IP 地址 `ws://8.155.148.75/ws` 连接
2. 当前 Nginx 配置没有 `/ws` 代理
3. 所以返回 502 错误

### 解决方案
编辑 `/etc/nginx/sites-enabled/` 下的配置文件，添加：
```nginx
server {
    listen 80 default_server;
    listen 443 ssl default_server;
    server_name _;
    
    location /ws {
        proxy_pass http://127.0.0.1:3010;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 7200s;
    }
}
```

### 配置文件位置
- `/etc/nginx/sites-enabled/` - Ubuntu 标准 Nginx 配置目录
- 保存后运行：`sudo nginx -t && sudo systemctl reload nginx`

---

## 2026-03-03 02:28 状态确认

### 用户反馈
用户反馈配置保存后仍然返回 502 错误。

### 新发现
通过 `find /etc -name "*.conf" 2>/dev/null | xargs grep -l "100000whys" 2>/dev/null` 发现配置文件。

### 当前配置目录
根据用户提供的配置，Nginx 配置应该在：
- `/etc/nginx/sites-enabled/`（Ubuntu 标准）
- 或宝塔路径 `/www/server/panel/vhost/nginx/`

### 解决方案
1. 确认 Nginx 配置文件实际位置
2. 添加 default_server 块和 `/ws` 代理
3. 重载 Nginx

---

## 2026-03-03 02:30 配置更新

### 用户新配置
用户提供了完整的 Nginx 配置文件，包含：
- default_server 块（处理 IP 地址请求）
- `/ws` WebSocket 代理
- 其他 API 代理

### 需要确认
1. 配置文件是否已保存到正确位置
2. Nginx 是否已重载

---

## HEARTBEAT_OK

## 2026-03-03 02:29 配置验证

### 用户配置
用户提供了完整的 Nginx 配置，包含 default_server 块和 `/ws` 代理。

### 新发现
配置文件实际位置是：
- `/etc/nginx/sites-available/xiaozong-proxy`（Ubuntu 标准 Nginx 配置）

原配置只有 8080 端口的代理，没有 `/ws` 代理。

### 解决方案
创建新配置文件 `/home/admin/openclaw/workspace/Nginx-Config-v2.txt`，包含：
- ✅ default_server 块（80/443 端口）
- ✅ `/ws` WebSocket 代理（3010 端口）
- ✅ `/api/` 反向代理（3001 端口）
- ✅ 保留原 8080 端口配置

### 操作步骤
```bash
# 备份原配置
sudo cp /etc/nginx/sites-available/xiaozong-proxy /etc/nginx/sites-available/xiaozong-proxy.bak

# 应用新配置
sudo cp /home/admin/openclaw/workspace/Nginx-Config-v2.txt /etc/nginx/sites-available/xiaozong-proxy

# 测试配置
sudo nginx -t

# 重载 Nginx
sudo systemctl reload nginx
```

### 需求变更（2026-03-03 02:33）
用户希望改用 PM2 管理服务。

### PM2 相关文件
1. **PM2 配置文件**：`/home/admin/openclaw/workspace/pm2-ws-bridge.json`
2. **Nginx 配置（宝塔版）**：`/home/admin/openclaw/workspace/Nginx-Baota-PM2.txt`
3. **完整指南**：`/home/admin/openclaw/workspace/PM2-GUIDE-BAOTA.md`

### 宝塔部署说明
- 公网 WebSocket 服务部署在宝塔服务器
- 用 PM2 管理 server.js（3010 端口）
- Nginx 添加 default_server 块处理 IP 请求

### 部署步骤
```bash
# 1. 安装 PM2
npm install -g pm2

# 2. 启动服务
cd /home/admin/openclaw/workspace
pm2 start pm2-ws-bridge.json
pm2 save
pm2 startup

# 3. 更新 Nginx（宝塔面板 → 100000whys.cn → 配置文件 → 添加 default_server 块）
# default_server 块内容见 Nginx-Baota-PM2.txt
sudo nginx -t && sudo systemctl reload nginx
```

### 完整部署文档
详见 `PM2-GUIDE-BAOTA.md`

---

## HEARTBEAT_OK

## 2026-03-03 02:55 问题定位

### 用户检查结果
1. **PM2 服务列表**：没有 ws-bridge 服务（3010 端口未监听）
2. **Nginx 配置**：`/etc/nginx/sites-available/xiaozong-proxy` 存在，但只有 8080 端口代理

### 问题定位
1. ❌ PM2 没有运行 ws-bridge 服务
2. ❌ Nginx 没有 `/ws` 代理
3. ❌ 没有 default_server 块处理 IP 请求

### 已完成修复
- ✅ Nginx 配置已更新（简化版，不使用 SSL）
- ✅ Nginx 配置测试通过

**当前状态**：
```bash
# Nginx 状态
nginx: configuration file /etc/nginx/nginx.conf test is successful

# PM2 问题
pm2 command not found

# 需要安装 PM2
sudo npm install -g pm2
```

### 端口说明
- **3010 端口**：WebSocket 服务（server.js）→ PM2 管理
- **3001 端口**：宝塔后端 API（不需要 PM2）

---

## 2026-03-03 02:56 状态确认

### PM2 配置文件
已创建：`/home/admin/openclaw/workspace/pm2-ws-bridge.json`

### Nginx 配置文件
已更新：`/etc/nginx/sites-available/xiaozong-proxy`（简化版，不使用 SSL）

### 待完成
1. 安装 PM2：`sudo npm install -g pm2`
2. 启动服务：`pm2 start pm2-ws-bridge.json`
3. 验证：`pm2 list` + `netstat -tlnp | grep 3010`

---

## HEARTBEAT_OK

## 2026-03-03 03:00 状态确认

### Nginx 配置
- ✅ 配置已更新（简化版，不使用 SSL）
- ✅ 配置测试通过：`nginx: configuration file /etc/nginx/nginx.conf test is successful`

### PM2 状态
- ⏳ 未安装，需要执行：`sudo npm install -g pm2`

### 待执行操作
1. 安装 PM2
2. 创建 PM2 配置文件：`/home/admin/openclaw/workspace/pm2-ws-bridge.json`
3. 启动服务：`pm2 start pm2-ws-bridge.json`
4. 验证：`pm2 list` + `netstat -tlnp | grep 3010`

### 成功标志
- PM2 运行 `ws-bridge` 服务（3010 端口）
- 风的 WebSocket 连接 `ws://8.155.148.75/ws` 成功

---

## HEARTBEAT_OK

## 2026-03-03 03:09 重要发现

### 项目路径确认
项目实际位置：`/home/admin/openclaw/workspace/skills/ws-bridge/`

### PM2 配置文件位置问题
- ❌ 之前配置文件可能在 `/root` 下（不正确）
- ✅ 应该在 `/home/admin/openclaw/workspace` 下（admin 用户项目目录）

### 正确路径结构
```
/home/admin/openclaw/workspace/
├── skills/
│   └── ws-bridge/
│       ├── server.js      # WebSocket 服务
│       ├── client.js      # 客户端
│       └── ...
├── pm2-ws-bridge.json     # PM2 配置文件（应该在这里）
└──memory/
    └── 2026-03-03.md      # 日记文件
```

### 待执行（admin 用户）
```bash
# 1. 切换到 admin 用户
su - admin

# 2. 创建 PM2 配置文件
cd /home/admin/openclaw/workspace
cat > pm2-ws-bridge.json << 'EOJSON'
{
  "apps": [{
    "name": "ws-bridge",
    "script": "/home/admin/openclaw/workspace/skills/ws-bridge/server.js",
    "instances": 1,
    "exec_mode": "fork",
    "env": {"NODE_ENV": "production"},
    "restart_delay": 30000
  }]
}
EOJSON

# 3. 安装 PM2 并启动
sudo npm install -g pm2
pm2 start pm2-ws-bridge.json
pm2 save

# 4. 验证
pm2 list
netstat -tlnp | grep 3010
```

---

## HEARTBEAT_OK

## 2026-03-03 03:14 重要更正 - 技术路线确认

### 正确的技术路线
1. **公网宝塔服务器** (8.155.148.75)
   - 部署 WebSocket 服务 (server.js)
   - 端口 3010
   - 需要 PM2 守护

2. **无影云电脑**
   - 运行 client.js 连接公网 3010
   - 项目路径: /home/admin/openclaw/workspace

### 问题定位
之前我错误地使用了无影云的路径 `/home/admin/openclaw/workspace`，但 WebSocket 实际部署在公网宝塔服务器上。

### 待确认
需要提供宝塔服务器上 server.js 的实际路径。

### 宝塔路径（待确认）
- /www/wwwroot/
- /www/server/
- /root/

### 用户操作
1. 在宝塔服务器上找到 server.js 的实际路径
2. 创建正确的 PM2 配置文件
3. 启动服务

---

## HEARTBEAT_OK

## 2026-03-03 03:19 技术路线澄清

### 最终确认的技术路线
1. **公网宝塔服务器** (8.155.148.75)
   - server.js (WebSocket 服务端，监听 3010 端口)
   - Nginx 代理配置
   - PM2 守护进程

2. **无影云电脑** (Wuying Cloud)
   - client.js (WebSocket 客户端，连接公网 3010)
   - 路径：`/www/wwwroot/100000whys.cn/project/fsj/`

### 之前错误
- 我错误地使用了无影云的路径 `/home/admin/openclaw/workspace`
- WebSocket 实际部署在公网宝塔服务器上

### 待确认
公网宝塔服务器上 `server.js` 的实际路径

### Wuying Cloud 当前问题（2026-03-03 03:19）
- client.js 缺少 `ws` 模块
- 需要安装：`npm install ws`

### 公网宝塔服务器待完成（2026-03-03 03:24）
1. server.js 路径待确认
2. 需要安装 PM2
3. 需要配置 PM2 守护 server.js

---

## HEARTBEAT_OK

## 2026-03-03 03:27 重要发现

### server.js 路径确认（公网宝塔服务器）
运行 `find / -name "server.js" -type f` 发现：
- `/www/wwwroot/100000whys.cn/project/sxz/backend/server.js` - sxz 项目
- `/www/wwwroot/100000whys.cn/project/sxz0/backend/server.js` - sxz0 项目
- `/www/wwwroot/100000whys.cn/project/fsj/merchantDashboard/server.js` - 商户后台
- `/www/wwwroot/api.100000whys.cn/server.js` - api 项目

这些都不是我们要用的 WebSocket server.js。

### WebSocket server.js 应该在哪里？
应该在 `/www/wwwroot/100000whys.cn/project/fsj/` 下，或者附近。

### 当前状态（Wuying Cloud）
- client.js 位置: `/www/wwwroot/100000whys.cn/project/fsj/pm2-ws-bridge.js`
- 缺少 ws 模块

### 公网宝塔服务器（8.155.148.75）
- server.js 路径待确认
- 需要运行 `find /www/wwwroot -name "server.js" -type f | xargs grep -l "3010"` 来找正确的

### 下一步
1. 在公网宝塔服务器上找到真正的 WebSocket server.js
2. 如果没有，需要上传 /home/admin/openclaw/workspace/skills/ws-bridge/server.js 到公网服务器
3. 创建 PM2 配置文件
4. 启动服务

---

## PM2 配置与部署（2026-03-03 03:39）

### 问题定位
用户需要在**公网宝塔服务器**上用 PM2 管理 WebSocket 服务（server.js）。

### server.js 路径确认
运行 `find / -name "server.js" -type f` 后发现：
- `/www/wwwroot/100000whys.cn/project/sxz/backend/server.js`
- `/www/wwwroot/100000whys.cn/project/sxz0/backend/server.js`
- `/www/wwwroot/100000whys.cn/project/fsj/merchantDashboard/server.js`
- `/www/wwwroot/api.100000whys.cn/server.js`

**都不是 WebSocket server.js**。

### WebSocket server.js 实际位置
根据对话上下文，WS server.js 应该在：
- `/www/wwwroot/100000whys.cn/project/fsj/server.js`（用户之前提到）

### PM2 配置文件路径问题
- ❌ 之前尝试在无影云创建 `/www/wwwroot/...` 路径失败（权限不足）
- ✅ 应该在公网宝塔服务器上创建：`/www/wwwroot/100000whys.cn/project/fsj/pm2-ws-bridge.json`

### 解决方案
在公网宝塔服务器（root 用户）下执行：
```bash
# 1. 确认 server.js 路径
ls -la /www/wwwroot/100000whys.cn/project/fsj/server.js

# 2. 写入 PM2 配置
tee /www/wwwroot/100000whys.cn/project/fsj/pm2-ws-bridge.json << 'EOJSON'
{
  "apps": [{
    "name": "ws-bridge",
    "script": "/www/wwwroot/100000whys.cn/project/fsj/server.js",
    "instances": 1,
    "exec_mode": "fork",
    "env": {"NODE_ENV": "production", "WS_PORT": "3010"},
    "restart_delay": 30000,
    "error_file": "/tmp/pm2-ws-bridge-error.log",
    "out_file": "/tmp/pm2-ws-bridge-out.log"
  }]
}
EOJSON

# 3. 安装 PM2
sudo npm install -g pm2

# 4. 启动服务
cd /www/wwwroot/100000whys.cn/project/fsj
pm2 start pm2-ws-bridge.json
pm2 save
pm2 startup

# 5. 验证
pm2 list
netstat -tlnp | grep 3010
```

### 当前状态（2026-03-03 03:39）
- ✅ WS 服务端代码：`/home/admin/openclaw/workspace/skills/ws-bridge/server.js`（无影云）
- ⏳ 公网部署：需要上传到 `/www/wwwroot/100000whys.cn/project/fsj/server.js`
- ⏳ PM2 配置：变量已给出，待在公网执行
- ⏳ Nginx：配置已更新（简化版，不使用 SSL），待重载

### 待用户操作（公网宝塔服务器 root 用户）
1. 上传 server.js 到 `/www/wwwroot/100000whys.cn/project/fsj/`
2. 运行上面 PM2 配置命令
3. 重载 Nginx：`sudo nginx -t && sudo systemctl reload nginx`
4. 测试风的 WebSocket 连接

---

## HEARTBEAT_OK

## 2026-03-03 03:50 重要更正 - 技术路线澄清

### 最终确认路径
**公网宝塔服务器** (8.155.148.75)：
- server.js: `/www/wwwroot/100000whys.cn/project/fsj/server.js`
- 端口：3010
- PM2 配置：`/www/wwwroot/100000whys.cn/project/fsj/pm2-ws-bridge.json`

### PM2 配置文件正确内容
```json
{
  "apps": [{
    "name": "ws-bridge",
    "script": "/www/wwwroot/100000whys.cn/project/fsj/server.js",
    "instances": 1,
    "exec_mode": "fork",
    "env": {"NODE_ENV": "production", "WS_PORT": "3010"},
    "restart_delay": 30000,
    "error_file": "/tmp/pm2-ws-bridge-error.log",
    "out_file": "/tmp/pm2-ws-bridge-out.log"
  }]
}
```

### 之前错误
- 我错误地在无影云尝试创建 `/www/wwwroot/...` 路径（权限不足）
- 应该**直接在公网宝塔服务器（root 用户）下操作**

### 成功标志
1. `pm2 list` 显示 `ws-bridge` 运行中
2. `netstat -tlnp | grep 3010` 显示端口监听
3. 风的 `ws://8.155.148.75/ws` 连接成功

---

## HEARTBEAT_OK
