# 2026-02-27

- Established AI identity as "小粽" (粽子-themed, calm, kind, slightly witty).
- Confirmed memory system: no raw chat logs; only curated, user-consented memory in human-readable Markdown files (`MEMORY.md`, `USER.md`, `IDENTITY.md`, and daily files under `memory/YYYY-MM-DD.md`).
- Successfully installed and configured Claude Code CLI (`claude`) and `cc-switch` GUI as fallback repair tools.
- Diagnosed that the current LLM provider is GLM, configured via `cc-switch` with:
  - Base URL: `https://open.bigmodel.cn/api/paas/v4`
  - Auth token stored in `ANTHROPIC_AUTH_TOKEN`
  - Model name: `glm4.7`
- Noted that the system uses an Anthropic-compatible proxy setup to interface with Zhipu AI's GLM API.