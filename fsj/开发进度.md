# 开发进度总览

> 约定：
> - `[x]` 表示已完成并可回归测试
> - `[ ]` 表示待开发或开发中
> - 发现需要补充的功能，在对应模块下增加新的待办项
> - 发现需要改进的点，在「改进记录」中追加说明和关联条目

---

## 一、已完成功能（可回归验证）

### 1. 门店装修基础框架

- [x] 搭建页面树 / 组件库 / 属性面板 / 预览区域四区布局
- [x] 支持组件拖拽到页面并生成页面组件列表
- [x] 支持组件在页面中的拖动排序
- [x] 支持删除组件并刷新页面树与预览
- [x] 支持页面列表的新增 / 删除 / 选中切换
- [x] 初始化默认页面（首页、我的等）及默认 tabBar 配置
- [x] 底部导航栏视图与全局设置视图、页面视图之间的切换逻辑

### 2. 组件系统与页面代码生成

- [x] 为轮播图、功能列表、图片、文本、商品网格、公告、标签页、内容列表等组件定义元信息
- [x] 为各组件配置默认属性（高度、列数、字体大小、颜色等）
- [x] 为各组件配置默认 componentItems（如轮播图默认三张图、功能列表默认三项等）
- [x] 在前端根据页面 JSON 生成页面组件配置（含 componentId、componentItems、properties）
- [x] 后端根据页面配置生成对应页面的 WXML / WXSS / JS / JSON 文件
- [x] 生成页面时为每个组件调用对应的模板生成函数（generateHTML / generateCSS 等）
- [x] 生成的小程序页面可正常渲染并输出基础日志用于调试

### 3. 全局样式与主题色联动

- [x] 在装修端提供全局主题色选择器（支持直接选色和 `{主题色}` 占位符）
- [x] 支持在导航栏背景色 / 文字色中使用 `{主题色}` 占位符
- [x] 支持在底部导航栏背景色 / 选中色 / 未选中色中使用 `{主题色}` 占位符
- [x] 在装修预览中实时应用全局主题色和相关占位符配置
- [x] 后端生成 app.json 时以 `globalConfig.themeColor` 作为主题色唯一数据源
- [x] 生成 app.json 时解析 `{主题色}` 占位符为实际主题色
- [x] 在写入 app.json 前，对所有颜色进行规范化（#RRGGBBAA / #RGB → #RRGGBB）
- [x] 移除 app.json 顶层 `globalStyle` 字段，统一使用 `window` 和 `tabBar` 管理全局样式

### 4. 底部导航栏逻辑与打包样式

- [x] 在装修页中渲染底部导航栏模拟器（图标 + 文本）
- [x] 点击底部导航栏项时，同步选中对应页面并切换预览内容
- [x] 在页面树中切换页面时，同步高亮对应的底部导航栏项
- [x] 从全局设置视图切换回底部导航栏视图时，默认选中首页
- [x] 后端在生成 app.json 时，根据 tabBar 配置生成 tabBar 列表
- [x] 将 tabBar 背景色 / 选中色 / 未选中色写入 app.json 中的 `tabBar` 字段
- [x] 设置 app.json 中的 `tabBar.borderStyle = "white"` 去除底部导航栏顶部灰色分隔线

### 5. 底部导航栏图标处理与主题色匹配

- [x] 在后端根据商家配置的图标 rowid，调用明道云图片映射下载图标到 `images` 目录
- [x] 为选中态 / 未选中态图标生成对应的本地文件名并写入 tabBar 配置
- [x] 使用 Jimp 对下载的图标进行主题色 / 自定义颜色叠加（mix 模式）
- [x] 使打包后小程序底部导航栏图标颜色与装修端设置的颜色保持一致
- [x] 在图标处理流程中增加日志输出，方便定位单个图标下载或处理失败的问题

### 6. 打包与下载流程（服务器端）

- [x] 在宝塔环境部署 Node.js + Express 打包服务
- [x] 使用 PM2 管理打包服务进程
- [x] 提供 `/api/generate-miniprogram` 接口接收页面配置 JSON
- [x] 复制 wxApp 基础框架代码（app 文件、utils、components 等）
- [x] 根据配置生成页面目录和代码文件（index.js / index.wxml / index.wxss / index.json）
- [x] 生成带主题色和 tabBar 配置的 app.json 文件
- [x] 使用 archiver 将输出目录打包为 ZIP 文件并返回给商家下载
- [x] 在打包过程中打印 app.json 构造体日志，便于远程调试

### 7. 明道云集成与图片映射

- [x] 封装 MingdaoYunAddAPI，用于新增数据行
- [x] 封装 MingdaoYunQueryAPI，用于按 rowid 查询单条数据
- [x] 封装 MingdaoYunUpdateAPI，用于更新数据行
- [x] 封装 MingdaoYunArrayAPI，用于按条件批量查询数据
- [x] 各 API 统一使用 `{success, data, error_msg, error_code}` 返回结构
- [x] 为明道云 API 封装实现自动重试、超时控制和详细日志
- [x] 在 wxApp 端提供 imageMapper 工具，将明道云图片字段解析为 `{rowid, url}` 数组
- [x] 在小程序启动时加载图片映射并写入 `App.globalData.imageMap`
- [x] 在页面中通过 `app.getImageUrl(rowid)` 获取实际图片 URL

### 8. 组件数据绑定 UI（通用 / 自定义）

- [x] 在组件属性面板中，用「数据绑定」替换原来的 JSON textarea 输入
- [x] 左侧下拉：数据来源类型（通用 / 自定义），默认选中「通用」
- [x] 右侧下拉：通用数据源（商品 / 文章 / 知识库）或自定义数据源占位（自定义数据）
- [x] 为组件配置新增 `dataSourceType` 字段（common / custom）
- [x] 为组件配置新增 `dataSourceKey` 字段（如 product / article / knowledge / custom）
- [x] 在保存配置时将上述数据绑定字段写入页面 JSON
- [x] 确保「数据绑定」样式在多个组件中可复用（通用样式）

### 9. 组件排序规则 UI

- [x] 在数据绑定UI下方添加排序规则下拉框
- [x] 实现 `getSortingOptionsHtml` 函数，根据数据来源类型动态生成排序选项
- [x] 实现 `updateComponentSorting` 函数，保存排序规则到组件配置
- [x] 通用数据来源显示：权重、发布时间正序、发布时间倒序、更新时间正序、更新时间倒序
- [x] 自定义数据来源显示：算法推荐
- [x] 排序规则保存到组件的 `sorting` 字段中

---

## 二、待开发功能（按最小单元拆分）

> 说明：以下均为规划中的进程，可按顺序逐项开发与测试。

### 1. 算法推荐能力接入（基于 neirong 表）

- [x] 设计并创建明道云中 `neirong` 表结构（内容 ID、各类基数、因子配置、权重等字段）
- [x] 约定 `neirong` 表与业务内容表（商品 / 文章 / 知识库 / 自定义表）的关联方式（如 rowid / 业务 ID）
- [ ] 实现每日统计行为数据（点赞、评论、收藏、完播、成交等）的批处理逻辑
- [ ] 将统计结果按标准化规则写入 `neirong` 各类「基数」字段
- [ ] 在商家后台提供配置页面，让商家设置各类「因子」权重（评论因子、收藏因子、主页因子等）
- [ ] 设计因子配置的存储结构（存入 `neirong` 或独立配置表）
- [ ] 在后端实现通用「权重计算」服务，按公式计算权重并回写 `neirong`
- [ ] 在小程序数据加载通道中增加「算法推荐模式」开关识别
- [ ] 在算法推荐模式下，先按权重从 `neirong` 表查询并排序得到内容 ID 列表
- [ ] 基于排序结果，按顺序从对应业务内容表中批量加载内容数据
- [ ] 将最终排序后的内容列表返回给小程序前端并渲染到组件中

### 2. 组件 JSON 与数据加载规则打通

- [ ] 在 `decoration.html` 中为列表类组件（商品列表 / 内容列表等）扩展配置字段：
  - 是否启用算法推荐
  - 目标数据表别名 / 控件标识
  - 排序字段 / 排序方向
  - 过滤条件（标签、分类、状态、时间范围等）
- [ ] 在组件属性面板中提供对应的表单控件以配置上述规则
- [ ] 在保存商家配置时，将数据加载规则写入页面 JSON（与 `dataSourceType` / `dataSourceKey` 一致管理）
- [ ] 在后端生成小程序页面 JS 时，解析组件数据加载配置
- [ ] 为每个列表组件在 `onLoad` / `onShow` 中注入对应的明道云数据请求逻辑
- [ ] 支持根据配置选择是否优先通过 `neirong` 做二次排序（算法模式）或直接按业务表排序（普通模式）

### 3. 通用数据 / 自定义数据表适配

- [ ] 定义通用数据（商品 / 文章 / 知识库等）对应的标准表结构与字段映射
- [ ] 为通用数据定义统一的「别名 → 表 / 控件」映射配置
- [ ] 约定自定义数据访问方式（例如通过「表别名 + 主键控件 ID」访问）
- [ ] 在后端封装一层统一的数据访问接口，屏蔽不同业务表结构差异
- [ ] 确保统一数据访问接口返回统一的数据结构（如 {id, title, cover, desc, price, tags, ...}）
- [ ] 在小程序组件渲染中仅依赖统一结构，减少对具体表结构的耦合

### 4. 组件级数据驱动渲染（列表类组件）

- [ ] 为商品列表组件设计数据驱动渲染模板（标题、封面、价格、销量等）
- [ ] 为内容列表组件设计数据驱动渲染模板（标题、封面、摘要、标签等）
- [ ] 在装修端为列表卡片提供展示字段配置（选择显示哪些字段、顺序、是否显示价格/销量等）
- [ ] 将字段展示配置写入组件 JSON
- [ ] 在小程序端根据组件配置动态控制字段显示和顺序
- [ ] 支持列表组件的分页加载与下拉刷新

### 5. 调试与监控

- [ ] 在后端增加算法推荐相关接口的详细日志（请求条件、排序结果、命中内容 ID 列表）
- [ ] 在后端为数据加载失败或返回空列表的情况输出告警日志
- [ ] 在小程序端为数据加载过程增加关键步骤日志（加载 `neirong`、加载业务表、最终渲染数量）
- [ ] 设计基础监控指标：推荐命中数、空列表比例、接口耗时等
- [ ] 为重要错误场景设计用户友好的前端提示文案

---

## 三、改进记录

> 用于记录已上线功能的优化与修正历史，便于后续追踪。

- 日期：2026-02-18
- 模块：图片处理架构
- 关联条目：### 7. 明道云集成与图片映射
- 问题描述：之前使用运行时图片映射（imageMapper.js）和初始化加载（initMerchant.js）的方式与打包时下载图片的方案重复，造成代码冗余
- 改进方案与效果：删除了 imageMapper.js 和 initMerchant.js，改为在打包时直接下载图片到 images 目录，小程序直接引用本地图片文件。同时将明道云数据调用方法整理为 skill 文件（明道云数据调用.md），供需要动态加载数据时参考。简化了代码结构，提高了性能。

