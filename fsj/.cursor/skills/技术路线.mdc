### webview
嵌入小程序的页面
采用html5+css+js技术，750px对应屏幕100%宽度rem布局，1rem=100px
使用语义化标签替代通用容器
为所有交互元素提供明确的标签
遵循HTML5标准规范
代码简洁，功能相似代码尽可能复用

#### UI
UI分像素UI和非像素UI
##### 像素UI
Canvas渲染，宽度120，高度撑满屏幕
游戏交互界面，地图、剧情和信息成列
变化多端、多样化、个性化的信息

##### 非像素UI
需要展示细节的界面，如商品、物品详情图、AI1对1对话场景
有规律的
复用型样式

### wxApp
原生小程序端，有常用工具、商城通用功能等，支持用户拖拉拽自动生成。功能比较冗余需要将代码上传到我们公司总后台（明道云），然后根据用户的需求进行定制。

#### 核心功能
- 可视化页面设计器（拖拉拽组件）
- JSON配置生成
- 代码打包下载
- 支持多种组件：轮播图、标签页面、功能导航栏、商品列表、内容列表等

#### 目录结构
```
wxApp/                          # 基础框架代码（git管理，同步到宝塔）
├── app.js                      # 小程序入口文件
├── app.wxss                    # 全局样式
├── app.json                    # 小程序配置（模板）
├── sitemap.json                # 索引配置
├── project.config.json         # 项目配置
├── utils/                      # 工具类
│   ├── request.js              # 网络请求封装
│   ├── storage.js              # 本地存储封装
│   └── auth.js                # 认证模块
├── components/                 # 通用组件
│   ├── carousel/               # 轮播图组件
│   │   ├── carousel.js
│   │   ├── carousel.wxml
│   │   ├── carousel.wxss
│   │   └── carousel.json
│   ├── function-list/          # 功能列表组件
│   │   ├── function-list.js
│   │   ├── function-list.wxml
│   │   ├── function-list.wxss
│   │   └── function-list.json
│   ├── single-image/          # 单图组件
│   │   ├── single-image.js
│   │   ├── single-image.wxml
│   │   ├── single-image.wxss
│   │   └── single-image.json
│   └── custom-text/           # 自定义文本组件
│       ├── custom-text.js
│       ├── custom-text.wxml
│       ├── custom-text.wxss
│       └── custom-text.json
└── templates/                  # 代码生成模板
    ├── page-template.js        # 页面JS模板
    ├── page-template.wxml      # 页面WXML模板
    ├── page-template.wxss      # 页面WXSS模板
    └── page-template.json      # 页面JSON模板
```

#### 混合打包方案
采用**混合打包模式**，结合基础框架代码和动态配置的优势：

1. **基础框架代码**（本地 wxApp 目录）
   - 通过 git 同步到宝塔
   - 包含通用组件、工具类、基础配置
   - 统一管理，便于维护和更新

2. **页面配置**（明道云）
   - 商家设计的页面 JSON 配置
   - 组件属性设置
   - 底部导航栏配置

3. **打包逻辑**（后端）
   - 复制基础框架代码
   - 根据配置生成页面代码
   - 生成 app.json（配置页面路径和 tabBar）
   - 打包成 ZIP

#### 打包流程
```
1. 后端接收 JSON 配置
   ↓
2. 复制 wxApp 基础框架代码
   ↓
3. 根据配置生成页面代码（使用模板）
   ↓
4. 生成 app.json（配置页面路径和 tabBar）
   ↓
5. 打包成 ZIP 文件
   ↓
6. 商家下载并导入微信开发者工具
```

#### 优势
- **灵活性**：商家可以自由设计页面，不受固定模板限制
- **可维护性**：基础框架代码统一管理，便于更新
- **性能**：基础代码直接复制，只生成页面代码，速度快
- **版本控制**：基础代码通过 git 管理，配置存储在明道云
- **可扩展**：新增组件只需更新模板，不影响现有功能

#### 底部导航栏样式
##### 风格1：经典风格
- 背景色：#ffffff
- 文字色：#999999（未选中）、#667eea（选中）
- 图标大小：48rpx
- 边框：顶部1px #e0e0e0
- 字体大小：24rpx

##### 风格2：现代风格
- 背景色：#f8f9fa
- 文字色：#6c757d（未选中）、#007bff（选中）
- 图标大小：52rpx
- 边框：无，使用阴影
- 字体大小：22rpx
- 圆角：8rpx

#### JSON成象
JSON成象是指将JSON数据转换为小程序页面的组件，每个组件都有自己的属性和方法。

##### 全局json
```json
[
    {
        "名称": "全局风格",
        "内容": "风格1"
    },
    {
        "名称": "全局导航栏",
        "内容": [
            {
                "背景色": "#000000",
                "文字色": "#ffffff"
            }
        ]
    }
]
```

##### 底部导航栏json
```json
[
    {
        "名称": "首页",
        "选中图标": "icon1.png",
        "未选中图标": "icon0.png",
        "指向页面": "529d0821-ea31-4588-9eea-cd5e0c98bc42"
    },
    {
        "名称": "分类",
        "选中图标": "icon1.png",
        "未选中图标": "icon0.png",
        "指向页面": "529d0821-ea31-4588-9eea-cd5e0c98bc42"
    }
]
```

##### 页面json
```json
[
    {
        "页面名称": "首页",
        "页面id": "529d0821-ea31-4588-9eea-cd5e0c98bc42",
        "页面json": [
            {
                "组件名称": "轮播图",
                "组件id": "529d0821-ea31-4588-9eea-cd5e0c98bc42",
                "组件项": ["5bdc6e9d-53ff-446b-80b4-461802349783","16e1d3ff-50e6-494c-ac07-4049183ba4f7"]
            },
            {
                "组件名称": "功能列表",
                "组件id": "529d0821-ea31-4588-9eea-cd5e0c98bc42",
                "组件项": ["圈子","想法","团送"]
            }
        ]
    },
    {
        "页面名称": "分类",
        "页面id": "529d0821-ea31-4588-9eea-cd5e0c98bc42",
        "页面json": ""
    }
]
```

**组件项（componentItems）数据格式说明：**
- **单条数据**：直接使用 rowid 字符串，如 `"5bdc6e9d-53ff-446b-80b4-461802349783"`
- **多条数据**：使用 rowid 数组，如 `["5bdc6e9d-53ff-446b-80b4-461802349783", "16e1d3ff-50e6-494c-ac07-4049183ba4f7"]`
- **静态文本内容**：对于文本组件等，仍可使用字符串数组，如 `["圈子","想法","团送"]`
- **数据来源**：小程序根据 rowid 从明道云的 `neirong` 表中加载实际内容数据

#### 组件模块化
拖拉拽定义模块，页面内容通过模块组合而成

##### 组件开发规范

每个组件需要实现以下方法：

```javascript
module.exports = {
  name: '组件名称',
  type: '组件类型',

  generateHTML(component) {
    // 生成小程序WXML代码
  },

  generateCSS() {
    // 生成小程序WXSS样式
  },

  getDefaultProperties() {
    // 返回默认属性配置
  },

  getDefaultItems() {
    // 返回默认内容项
  },

  getPropertyLabels() {
    // 返回属性标签映射
  },

  async loadData(merchantId, outputDir) {
    // 系统级组件实现此方法，从明道云加载数据
    // 返回格式化后的数据数组
    // 可选方法，用户级组件不需要实现
  }
};
```

**组件职责划分**：
- **系统级组件**（轮播图、功能列表、商品网格、内容列表）：必须实现 `loadData` 方法
- **用户级组件**（图片、文本、公告、标签页）：不需要实现 `loadData` 方法

**loadData 方法说明**：
- 在打包时由后端调用
- 负责从明道云加载组件数据
- 下载相关图片到 `images` 目录
- 返回格式化后的数据，供 `generateHTML` 使用

##### 可用组件列表
1. 轮播图（carousel）
2. 功能列表（function-list）
3. 图片（image）
4. 文本（text）
5. 商品网格（product-grid）
6. 公告（notice）
7. 标签页面（tabs）
8. 内容列表（content-list）

#### json规范
1. 凡同级别下多个模块，必须用数组包裹
2. 数组内元素的顺序即为页面渲染的顺序，无需额外排序字段
3. **拖动排序原理**：前端实现拖动排序时，直接交换数组中元素的索引位置即可。保存时提交新的数组顺序，不需要在数据对象中额外添加排序字段。

### merchantDashboard
商家端后台，页面作为自定义视图嵌入明道云

#### 核心模块
1. 首页（shouye）- 店铺信息、运营数据、常用功能、营销功能、代码发布
2. 店铺管理（store）- 店铺资料、门店管理、下单设置、更多设置、图片空间
3. 门店装修（decoration）- 页面设计器，拖拉拽组件设计小程序页面
4. 商品管理（product）
5. 订单管理（order）
6. 配送管理（delivery）
7. 会员管理（member）
8. 智慧营销（marketing）
9. 圈子管理（circle）
10. 内容管理（content）
11. 系统设置（system）

#### 数据交互
- 根据url参数显示不同用户的数据
- 获取商家id：?mRowid=123456
- 通过明道云API获取商家数据
- 数据存储在localStorage中供页面使用

#### 门店装修功能
##### 页面树概念
- 左侧：页面树（页面列表）
- 中间：组件库（可拖拉拽的组件）
- 右侧：属性面板（选中组件的属性设置）
- 底部：预览区域（实时预览小程序页面效果）

##### 视图切换
- **页面视图**：显示页面列表和组件库
- **底部导航栏视图**：显示底部导航项列表和属性配置
- **全局设置视图**：显示全局样式配置（主题色、导航栏样式等）

##### 全局设置功能
- **主题色选择**：设置小程序全局主题色（影响按钮、链接等元素）
- **导航栏配置**：
  - 背景色设置
  - 文字色设置
- **底部导航栏配置**：
  - 背景色设置
  - 选中色设置
  - 未选中色设置
 - 颜色占位符 `{主题色}` 支持：
   - 导航栏背景色、文字色
   - 底部导航栏背景色、选中色、未选中色
   - 打包生成 app.json 时，这些字段如配置为 `{主题色}` 会自动替换为全局主题色

##### 组件拖拉拽
- 从组件库拖拽组件到页面
- 支持组件排序（拖动排序）
- 支持组件删除
- 支持组件属性编辑

##### "我的"页面的特殊性
- **特殊页面**："我的"页面是一个特殊页面，不同于其他页面（首页、分类等）
- **组件筛选**：当在页面树中选中"我的"页面时，组件库中只显示与"我的"页面相关的特殊组件
- **特殊组件列表**：
  - 我的订单
  - 会员权益消耗明细
  - 登录用户协议
  - 新手引导
  - 设置
  - 其他商家后台相关功能
- **实现原理**：
  - 在组件库中为每个组件添加 `relatedPage` 属性，标识该组件属于哪个页面
  - 当选中"我的"页面时，只显示 `relatedPage === 'my'` 的组件
  - 其他页面（首页、分类等）显示 `relatedPage === 'home'` 或 `relatedPage === 'category'` 的组件
  - 这样可以确保不同页面的组件不会混淆，提升用户体验

##### 代码生成流程
1. 商家通过可视化编辑器设计页面
2. 生成JSON配置文件
3. 调用后端API生成小程序代码
4. 打包成ZIP文件
5. 商家下载代码包
6. 使用微信开发者工具导入并上传

##### 数据绑定与算法推荐（规划中）

- **组件级数据绑定 JSON**  
  - `decoration.html` 中的组件配置会生成一段渲染 JSON，用于描述：  
    - 数据来源类型：`dataSourceType`（`common` 通用 / `custom` 自定义）  
    - 数据来源表：`dataSourceKey` 或表别名（商品、文章、知识库、自定义数据等）  
    - 排序规则：按字段、方向、是否分页等  
    - 过滤条件：标签、分类、状态、时间范围等  
  - 该 JSON 将作为小程序端加载数据的唯一配置入口。

- **通用数据 vs 自定义数据**  
  - **统一数据源**：所有内容（商品、文章、知识库、活动等）统一存储在 `neirong` 数据表
  - **统一结构**：`neirong` 表包含通用字段（标题、描述、封面、详情图、价格、作者、发布时间等），支持不同内容类型共用
  - **类型区分**：通过 `leixing`（类型）字段区分不同内容（商品、文章、知识库等）
  - **自定义数据**：商家在明道云中自行创建的内容，同样存入 `neirong` 表
  - 在 UI 上使用统一的「通用 / 自定义」双下拉样式进行选择，便于在不同模块复用。

- **排序规则与明道云计算**
  - 所有排序（包括权重排序）由明道云侧完成，小程序与 Node 只负责：
    - 在组件 JSON 中声明「排序方式」
    - 将排序配置透传给明道云 API
    - 按明道云返回的顺序渲染数据
  - 通用数据（`dataSourceType = common`）排序选项：
    - `weight`：按权重排序（使用 `neirong` 表的权重字段）
    - `publishTimeAsc`：发布时间正序
    - `publishTimeDesc`：发布时间倒序
    - `updateTimeAsc`：更新时间正序
    - `updateTimeDesc`：更新时间倒序
  - 自定义数据（`dataSourceType = custom`）排序选项：
    - `algorithm`：算法推荐模式（商家自行根据下面的公式在明道云中 DIY 排序逻辑）

- **算法推荐模式（使用 `neirong` 表）**
  - 当组件启用算法推荐（排序方式为 `weight` 或 `algorithm`）模式时：
    - 所有内容的权重计算和排序逻辑在明道云中实现
    - 小程序/后端仅携带必要参数（如商家 ID、内容类型等）调用明道云接口
    - 明道云在 `neirong` 表中维护权重字段，并按权重排序返回内容（或内容 ID 列表）
  - 规划中的权重计算公式（在明道云中实现）：  
    - 权重 = 评论基数 × 评论因子  
      + 收藏基数 × 收藏因子  
      + 主页转化基数 × 主页因子  
      + 标签基数 × 标签因子  
      + 完播基数 × 完播因子  
      + 评分基数 × 评分因子  
      + 加权基数 × 加权因子  
      + 成交基数 × 成交因子  
      + 发布时间 × 发布因子  
  - 因子：由商家在后台（或明道云配置表）自行配置的权重系数  
  - 基数：系统每日统计计算的标准化数据，例如：  
    - 点赞基数 = 当前内容的点赞数 ÷ （所有内容总点赞数 ÷ 内容总数）  
    - 其他行为（评论、收藏、完播、成交等）以类似方式进行标准化  
  - 推荐流程（规划）：
    1. 明道云每天统计行为数据，更新 `neirong` 表中的各类基数字段
    2. 明道云根据商家配置的因子，按公式计算权重并写入 `neirong` 表
    3. 小程序侧发起数据请求时，带上排序方式（如权重排序）
    4. 明道云根据 `neirong` 中的权重和排序方式返回排序后的数据或数据 ID 列表
    5. 小程序按返回顺序渲染到对应组件（商品列表、内容列表等）中

- **非算法模式（直接按业务表排序）**
  - 如果组件未启用算法推荐：
    - 明道云直接从对应业务表（商品 / 文章 / 知识库 / 自定义）中查询数据
    - 按组件 JSON 中声明的排序方式（如发布时间倒序、更新时间正序、手动排序字段等）进行排序
  - 过滤与分页规则同样通过组件 JSON 配置描述，并由明道云 API 按规则执行。

- **当前状态说明**
  - 组件数据绑定 UI（通用 / 自定义下拉）以及组件 JSON 生成已实现
  - 排序规则字段（通用排序 / 自定义算法标识）已在组件配置中预留
  - 实际的权重计算与排序在明道云侧实现，当前文档记录约定与数据流，便于后续迭代实现

##### 成功经验：WXML 编译错误修复（`wx:for`）

- **问题现象**：打包后的小程序在微信开发者工具中提示  
  `Bad attr wx:for-items`，定位到 WXML 中存在 `wx:for="{{轮播图}}"` 这类中文变量名。
- **根因**：WXML 表达式不接受中文变量名，且打包生成时使用了 `componentName`（中文）作为数据字段。
- **修复方式**：  
  1. 在打包生成逻辑中统一使用英文数据字段（如 `carouselImages` / `functionList`）。  
  2. 生成页面 JS 时使用同名字段写入 `data` 和 `setData`。  
  3. 组件生成 WXML 时通过 `dataKey` 透传字段名，避免硬编码中文变量。  
- **验证路径**：重新打包 → 导入开发者工具 → `index.wxml` 中 `wx:for` 变为英文字段 → 编译错误消失。  
- **注意事项**：涉及打包生成逻辑修改后，必须重启 PM2 并重新打包才能生效。

##### 成功经验：手机号一键登录与商家小程序配置

- **问题现象**：打包小程序的「手机号一键登录」点击后返回 `invalid code`，微信接口提示 code 无效。
- **根因**：用于 `jscode2session` 的 AppID/Secret 与当前小程序的 AppID 不一致。打包小程序的 `project.config.json` 中的 appid（如 `wx8899213e89e3cd45`）必须与调用微信接口时使用的 appid 完全一致，否则会出现 `invalid code`。
- **架构要点**：
  1. **配置来源**：支持多种配置源，按优先级使用：
     - 明道云 / `wechat-credentials.json`（通过 server.js `getMerchantWechatConfig`）
     - MySQL `merchant_wx_config` 表（通过 wxApp.js `getMerchantConfig`）
  2. **请求路径**：小程序 → `https://api.100000whys.cn/api/core/api/phone-login` → Nginx → wxApp:3000 或 server → proxy → wxApp
  3. **转发注入**：server.js 在转发 /api/core/api/phone-login 前，根据 merchantId 调用 `getMerchantWechatConfig`，若拿到 appId/secret 则注入到请求体，wxApp 优先使用请求体中的 appId/appSecret。
- **修复方式**：
  1. **MySQL 配置**：在宝塔 MySQL 的 `merchant_wx_config` 表中，为每个 merchant_id 添加对应小程序的 appid、secret：
     ```sql
     INSERT INTO merchant_wx_config (merchant_id, appid, secret) 
     VALUES ('698826f3b35652a8d4f60e21', 'wx8899213e89e3cd45', '你的小程序AppSecret');
     ```
  2. **小程序端**：点击「手机号一键登录」后先执行 `wx.login` 获取 loginCode，再与 getPhoneNumber 的 code 一起发送；请求体必须包含 `merchantId`。
  3. **wxApp 日志**：`[getMerchantConfig]` 打印 MySQL 配置读取结果，`[phone-login]` 打印使用的 appid 和返回的 openId，便于排查。
- **验证路径**：MySQL 正确配置 → 重启 wxApp → 小程序点击手机号一键登录 → 控制台出现「从 MySQL 读取到配置」「成功返回 openId」→ 登录成功。
- **注意事项**：`merchant_wx_config.appid` 必须与打包小程序的 `project.config.json` 中 appid 一致；Secret 在微信公众平台-开发管理-开发设置中获取。

### 后端架构（宝塔部署）

#### 技术栈
- Node.js + Express
- 代码生成引擎（基于模板）
- 文件打包（archiver）

#### 核心API
1. POST /api/generate-miniprogram - 生成小程序代码
   - 接收：JSON配置
   - 返回：ZIP文件

2. POST /api/save-config - 保存页面配置
   - 接收：JSON配置
   - 返回：保存结果

3. GET /api/load-config - 加载页面配置
   - 接收：商家ID
   - 返回：JSON配置

#### 部署说明
- 部署在宝塔面板
- 使用PM2管理Node.js进程
- 配置Nginx反向代理
- 支持HTTPS

#### 服务器端口与路径对应关系

| 端口 | 进程名称 | 路径 | 功能 |
|------|---------|------|------|
| 3000 | server.js | `/www/wwwroot/api.100000whys.cn` | 登录接口（wxApp.js） |
| 3001 | server.js | `/www/wwwroot/100000whys.cn/project/fsj/merchantDashboard` | 小程序打包生成 |

#### 后端打包逻辑示例

```javascript
// 复制基础框架代码
async function copyBaseFramework(outputDir) {
    const baseDir = path.join(__dirname, '../wxApp');
    const filesToCopy = [
        'app.js',
        'app.wxss',
        'sitemap.json',
        'project.config.json',
        'utils',
        'components'
    ];
    
    for (const item of filesToCopy) {
        const src = path.join(baseDir, item);
        const dest = path.join(outputDir, item);
        
        if (fs.existsSync(src)) {
            if (fs.statSync(src).isDirectory()) {
                fs.cpSync(src, dest, { recursive: true });
            } else {
                fs.copyFileSync(src, dest);
            }
        }
    }
}

function resolveThemeColor(value, themeColor) {
    if (!value) {
        return value;
    }
    if (value === '{主题色}') {
        return themeColor;
    }
    return value;
}

function normalizeColorForAppJson(color) {
    if (!color || typeof color !== 'string') {
        return color;
    }
    let c = color.trim();
    if (/^#([0-9a-fA-F]{8})$/.test(c)) {
        c = '#' + c.slice(1, 7);
    } else if (/^#([0-9a-fA-F]{3})$/.test(c)) {
        const r = c[1];
        const g = c[2];
        const b = c[3];
        c = `#${r}${r}${g}${g}${b}${b}`;
    }
    return c;
}

// 根据JSON配置生成页面代码
async function generatePage(page, outputDir) {
    const pageDir = path.join(outputDir, 'pages', page.pageId);
    fs.mkdirSync(pageDir, { recursive: true });
    
    const jsContent = renderTemplate('page-template.js', page);
    const wxmlContent = renderTemplate('page-template.wxml', page);
    const wxssContent = renderTemplate('page-template.wxss', page);
    
    fs.writeFileSync(path.join(pageDir, 'index.js'), jsContent);
    fs.writeFileSync(path.join(pageDir, 'index.wxml'), wxmlContent);
    fs.writeFileSync(path.join(pageDir, 'index.wxss'), wxssContent);
}

// 生成app.json（带主题色占位符解析和颜色规范化）
async function generateAppJson(config, outputDir) {
    const themeColorRaw = config.globalConfig.themeColor || '#667eea';
    const themeColor = normalizeColorForAppJson(themeColorRaw) || '#667eea';

    const navBgRaw = resolveThemeColor(config.globalConfig.navigationBar.backgroundColor, themeColor);
    const navBg = normalizeColorForAppJson(navBgRaw) || '#ffffff';

    const navTextRaw = resolveThemeColor(config.globalConfig.navigationBar.textColor, themeColor);
    const navText = normalizeColorForAppJson(navTextRaw) || '#181818';

    const navigationBarTextStyle = navText && navText.toLowerCase() === '#ffffff' ? 'white' : 'black';

    const appJson = {
        pages: config.pages.map(p => `pages/${p.pageId}/index`),
        window: {
            backgroundTextStyle: 'light',
            navigationBarBackgroundColor: navBg,
            navigationBarTitleText: '小程序',
            navigationBarTextStyle
        },
        sitemapLocation: 'sitemap.json'
    };

    if (config.tabBarConfig.list.length > 0) {
        const tabBgResolved = resolveThemeColor(config.tabBarConfig.backgroundColor || '#ffffff', themeColor);
        const tabBg = normalizeColorForAppJson(tabBgResolved) || '#ffffff';

        const unselectedResolved = resolveThemeColor(config.tabBarConfig.unselectedColor || '#999999', themeColor);
        const unselected = normalizeColorForAppJson(unselectedResolved) || '#999999';

        const selectedResolved = resolveThemeColor(config.tabBarConfig.selectedColor, themeColor);
        let selected = normalizeColorForAppJson(selectedResolved);
        if (!selected) {
            selected = themeColor;
        }

        appJson.tabBar = {
            color: unselected,
            selectedColor: selected,
            backgroundColor: tabBg,
            borderStyle: 'white',
            list: config.tabBarConfig.list.map(tab => ({
                pagePath: `pages/${tab.pageId}/index`,
                text: tab.name,
                iconPath: `images/${tab.unselectedIcon}`,
                selectedIconPath: `images/${tab.selectedIcon}`
            }))
        };
    }
    
    fs.writeFileSync(path.join(outputDir, 'app.json'), JSON.stringify(appJson, null, 2));
}
```

**实现要点总结：**
- 以 `globalConfig.themeColor` 作为全局主题色单一数据源
- 支持在导航栏和底部导航栏颜色字段中使用占位符 `{主题色}`，生成时统一替换为主题色
- 写入 `app.json` 前，对所有颜色进行规范化：
  - 将 `#RRGGBBAA` 转换为 `#RRGGBB`
  - 将 `#RGB` 转换为 `#RRGGBB`
- `app.json` 顶层不再使用 `globalStyle` 字段，统一通过 `window` 和 `tabBar` 配置全局样式
- `tabBar.borderStyle` 在生成时固定为 `white`，以去除底部导航栏顶部的灰色分隔线

#### 数据加载架构

##### 方案演进历程

**初始方案：服务器端加载数据（已废弃）**
- **设计思路**：在打包时由后端调用明道云 API 获取组件数据，将数据直接写入生成的小程序代码中
- **实现方式**：
  - 在 `server.js` 中调用组件的 `loadData` 方法
  - 组件从明道云 API 获取数据后，返回格式化后的数据
  - 后端将数据直接写入生成的页面 JS 文件中
  - 小程序启动时直接使用打包时写入的数据
- **失败原因**：
  1. **API 调用失败**：服务器端调用明道云 API 时多次返回空数据，原因不明
  2. **过滤条件问题**：初始使用了 `use` 字段过滤，导致数据被过滤掉
  3. **调试困难**：无法在微信开发者工具中查看 API 调用过程，只能通过服务器日志排查
  4. **效率低下**：每次打包都需要重新调用 API 获取数据，耗时且不必要
  5. **数据不实时**：打包后数据就固定了，需要重新打包才能更新数据

**最终方案：小程序端自行加载数据（当前方案）**
- **设计思路**：在生成的小程序页面代码中添加数据加载方法，小程序启动时直接调用明道云 API 获取数据
- **实现方式**：
  1. **后端生成数据加载代码**：
     - 在 `server.js` 的 `generatePageJS` 函数中，为每个组件生成对应的数据加载方法
     - 生成 `load{组件名}Data()` 方法：负责调用 API 并更新页面数据
     - 生成 `call{组件名}API()` 方法：负责调用明道云 API 并处理返回结果
  2. **小程序端调用数据加载**：
     - 在页面的 `onLoad` 生命周期中调用各个组件的 `load{组件名}Data()` 方法
     - 每个组件独立加载数据，互不影响
  3. **数据映射处理**：
     - 在 `call{组件名}API()` 方法中对 API 返回的数据进行映射和格式化
     - 例如轮播图组件：从 `img` 字段解析图片 URL，从 `url` 字段获取直接链接
  4. **错误处理**：
     - 每个 `load{组件名}Data()` 方法都有 try-catch 错误捕获
     - 加载失败时输出错误日志，但不影响其他组件加载

**方案对比**：

| 对比项     | 服务器端加载                 | 小程序端加载                 |
| ---------- | ---------------------------- | ---------------------------- |
| 数据实时性 | 打包后固定，需重新打包       | 每次启动都获取最新数据       |
| 打包速度   | 慢（需要调用多个 API）       | 快（只生成代码）             |
| 调试难度   | 高（只能看服务器日志）       | 低（可在微信开发者工具查看） |
| 灵活性     | 低（数据固定）               | 高（数据实时更新）           |
| 依赖性     | 依赖服务器网络环境           | 依赖小程序网络环境           |
| 维护成本   | 高（每次数据更新需重新打包） | 低（数据自动更新）           |

##### 技术要点

**1. 数据加载方法生成**

在 `server.js` 的 `generatePageJS` 函数中，为每个组件生成数据加载代码：

```javascript
const loadMethods = components.map(comp => {
    let worksheetId = '';
    let dataMapping = '';
    
    if (comp.componentName === '轮播图') {
        worksheetId = 'lunbotu';
        dataMapping = `result.data.rows.map(row => {
            let url = '';
            if (row.url) {
                url = row.url;
            } else if (row.img) {
                try {
                    const imgArray = JSON.parse(row.img);
                    if (Array.isArray(imgArray) && imgArray.length > 0) {
                        url = imgArray[0].large_thumbnail_full_path || imgArray[0].url;
                    }
                } catch (e) {
                    console.error('解析img字段失败:', e);
                }
            }
            return { url };
        }).filter(item => item.url)`;
    } else if (comp.componentName === '功能列表') {
        worksheetId = 'gongnengliebiao';
        dataMapping = `result.data.rows.map(row => ({
            icon: row.icon,
            name: row.name
        }))`;
    }
    
    return `
    async load${comp.componentName}Data() {
        try {
            console.log('开始加载${comp.componentName}数据...');
            const app = getApp();
            const merchantId = app && app.globalData && app.globalData.merchantId ? app.globalData.merchantId : '${merchantId || ''}';
            
            const data = await this.call${comp.componentName}API(merchantId);
            console.log('${comp.componentName}数据加载成功:', data);
            this.setData({ ${comp.componentName}: data });
        } catch (error) {
            console.error('${comp.componentName}数据加载失败:', error);
        }
    },
    
    async call${comp.componentName}API(merchantId) {
        const api = require('../../utils/MingdaoYunArrayAPI');
        const apiInstance = new api();
        
        const result = await apiInstance.getData({
            worksheetId: '${worksheetId}',
            filters: [],
            pageSize: 50,
            pageIndex: 1
        });
        
        if (result.success && result.data && result.data.rows) {
            return ${dataMapping};
        }
        return [];
    },`;
}).join('\n');
```

**2. 页面生命周期调用**

在生成的页面 JS 中，在 `onLoad` 生命周期中先调用 `initShangjiaRowid` 获取商家实际 rowid，再调用数据加载方法：

```javascript
async onLoad(options) {
    console.log('${page.pageName}页面加载', options);
    const app = getApp();
    const appMerchantId = app && app.globalData && app.globalData.merchantId ? app.globalData.merchantId : '${merchantId || ''}';
    console.log('商家ID:', appMerchantId);
    console.log('小程序版本: ${MINIPROGRAM_VERSION}');
    
    await this.initShangjiaRowid(appMerchantId);
    
    console.log('=== 开始加载组件数据 ===');
${loadDataCalls}
    console.log('=== 组件数据加载结束 ===');
},
```

**3. 数据映射处理**

不同组件的数据结构不同，需要在 `call{组件名}API()` 方法中进行数据映射：

- **轮播图组件**：
  - API 返回的 `fengmian` 字段是 JSON 字符串，需要解析为数组
  - 从数组中提取第一张图片的 URL
  - 优先使用 `url` 字段，如果不存在则从 `fengmian` 字段解析
  - 注意：轮播图数据表中的图片字段键名是 `fengmian`，不是 `img`

- **功能列表组件**：
  - API 返回的数据包含 `icon` 和 `name` 字段
  - 直接映射为 `{ icon, name }` 格式

**4. 错误处理**

每个数据加载方法都有完整的错误处理：

```javascript
async load轮播图Data() {
    try {
        console.log('开始加载轮播图数据...');
        const app = getApp();
        const merchantId = app && app.globalData && app.globalData.merchantId ? app.globalData.merchantId : '${merchantId || ''}';
        
        const data = await this.call轮播图API(merchantId);
        console.log('轮播图数据加载成功:', data);
        this.setData({ 轮播图: data });
    } catch (error) {
        console.error('轮播图数据加载失败:', error);
    }
},
```

**5. 版本号管理**

为了追踪代码版本，在 `server.js` 中定义版本号常量：

```javascript
const MINIPROGRAM_VERSION = '1.0.3';
```

在生成的页面 JS 中打印版本号：

```javascript
console.log('小程序版本: ${MINIPROGRAM_VERSION}');
```

每次修改代码时更新版本号，便于追踪问题和验证更新是否生效。

##### 关键技术点总结

1. **去过滤条件**：不使用任何过滤条件，直接获取所有数据，避免因过滤条件导致数据为空
2. **数据映射**：在 `call{组件名}API()` 方法中对 API 返回的数据进行映射和格式化
3. **错误处理**：每个数据加载方法都有 try-catch 错误捕获，不影响其他组件加载
4. **日志输出**：在关键步骤输出日志，便于调试和排查问题
5. **版本管理**：使用版本号追踪代码版本，便于问题定位
6. **模块化设计**：每个组件独立加载数据，互不影响，便于维护和扩展

##### 失败经验总结

1. **不要在服务器端加载数据**：服务器端加载数据会导致数据不实时、打包速度慢、调试困难等问题
2. **避免使用过滤条件**：过滤条件可能导致数据被过滤掉，建议先获取所有数据，再在前端过滤
3. **充分的日志输出**：在关键步骤输出日志，便于调试和排查问题
4. **版本号管理**：使用版本号追踪代码版本，便于问题定位和验证更新是否生效
5. **小程序 API 兼容性**：注意 Node.js 和微信小程序 API 的差异，确保代码在小程序环境中正常运行

### 开发进程

#### 已实现功能（可回归验证）

- 门店装修基础框架
  - 页面树 / 组件库 / 属性面板 / 预览区域布局完成
  - 底部导航栏视图与全局设置视图切换逻辑完成
  - 初始化默认页面（首页 / 我的）及默认 tabBar 配置

- 组件系统与页面生成
  - 组件元信息和默认属性（轮播图、功能列表、图片、文本、商品网格、公告、标签页、内容列表）配置完成
  - 前端拖拽生成的页面 JSON 可传入后端，生成对应的 WXML / WXSS / JS / JSON
  - 生成的小程序页面支持基本数据渲染和生命周期日志输出

- 全局样式与主题色
  - 可视化全局主题色选择器（含 `{主题色}` 占位符按钮）完成
  - 导航栏背景色 / 文字色与全局主题色联动，并在装修预览中实时生效
  - 底部导航栏选中色 / 未选中色 / 背景色支持 `{主题色}` 占位符
  - 后端生成 app.json 时解析 `{主题色}` 并进行颜色规范化（#RRGGBBAA、#RGB → #RRGGBB）
  - 移除 app.json 顶层 `globalStyle` 字段，避免微信开发者工具报「无效的 app.json」

- 底部导航栏逻辑
  - 装修页底部导航栏与页面树实现双向联动（选中 tab → 切换页面，切换页面 → 对应 tab 高亮）
  - 默认选中首页逻辑（初始加载、从全局视图切回底部导航视图）完成
  - 打包时根据 tabBar 配置生成 app.json 中的 tabBar 列表
  - 下载明道云中配置的导航图标并写入 images 目录
  - 使用主题色 / 自定义颜色对选中 / 未选中图标进行颜色叠加，保持与 tabBar 文本颜色风格一致
  - 生成 app.json 时将 `borderStyle` 固定为 `white`，去除底部灰色分隔线

- 打包与下载流程
  - 后端打包服务（Node.js + Express + archiver）部署在宝塔，提供 `/api/generate-miniprogram` 接口
  - 复制 wxApp 基础框架代码 + 生成页面代码 + 生成 app.json + 打包 ZIP 的流程打通
  - 在装修页点击「生成小程序代码」后，可下载并导入微信开发者工具运行
  - 添加 app.json 构造体日志输出，便于在服务器端排查配置问题

- 明道云集成
  - MingdaoYunArrayAPI / AddAPI / QueryAPI / UpdateAPI 封装完成，统一返回 `{success, data, error_msg, error_code}`
  - 图片处理采用打包时下载方案，所有图片在打包时下载到 `images` 目录
  - 小程序直接引用本地图片文件，无需运行时获取
  - 明道云数据调用方法已整理为 skill 文件（`明道云数据调用.md`），供需要动态加载数据时参考

- 数据绑定 UI（组件级）
  - 属性面板中原来的组件内容 textarea 改为通用的「数据绑定」双下拉样式
  - 左侧下拉：数据来源类型（通用 / 自定义），默认选中「通用」
  - 右侧下拉：具体数据源（商品 / 文章 / 知识库 / 自定义数据）
  - 组件配置中新增 `dataSourceType` / `dataSourceKey` 字段，并写入保存的 JSON 配置
  - 排序规则配置：
    - **通用数据来源**：提供标准排序选项
      - 权重（按内容权重排序，适用于推荐场景）
      - 发布时间正序（从旧到新）
      - 发布时间倒序（从新到旧）
      - 更新时间正序（从旧到新）
      - 更新时间倒序（从新到旧）
    - **自定义数据来源**：提供算法推荐排序选项
      - 算法推荐（基于 `neirong` 表的权重计算）
      - 商家可配置各类因子权重（评论因子、收藏因子、主页因子等）
      - 支持自定义排序规则（基于商家设定的因子权重组合）
  - **已实现**：
    - 在 `decoration.html` 中添加排序规则下拉框UI
    - 实现 `getSortingOptionsHtml` 函数，根据数据来源类型动态生成排序选项
    - 实现 `updateComponentSorting` 函数，保存排序规则到组件配置的 `sorting` 字段
    - 排序规则会根据数据来源类型自动切换显示对应的选项

#### 待开发功能（按最小单元拆分）

- 算法推荐能力接入（基于 `neirong` 表）
  - 设计并创建明道云中 `neirong` 表结构（内容 ID、各类基数字段、因子配置、最终权重等）
  - 实现每天统计行为数据（点赞、评论、收藏、完播、成交等）的批处理逻辑，写入 `neirong` 基数字段
  - 支持在商家后台配置各类「因子」权重（评论因子、收藏因子、主页因子等），写入 `neirong` 或相关配置表
  - 在后端实现通用的「权重计算」服务，根据基数和因子计算最终权重并回写 `neirong`
  - 在小程序数据加载通道中识别「算法推荐」模式，优先查询 `neirong` 表并按权重排序
  - 基于 `neirong` 排序结果，按顺序到具体业务表（商品 / 文章 / 知识库 / 自定义）中加载内容

- 组件 JSON 与数据加载规则打通
  - 在 `decoration.html` 中为需要列表数据的组件（商品列表 / 内容列表等）扩展数据加载配置字段：  
    - 是否启用算法推荐  
    - 目标数据表别名 / 控件标识  
    - 排序字段 / 排序方向  
    - 过滤条件（标签、分类、时间等）
  - 在保存配置时，将上述数据加载规则写入商家配置 JSON 中
  - 在后端生成小程序页面 JS 时，根据组件的数据加载规则生成对应的 `onLoad` / `onShow` 数据请求逻辑
  - 在小程序运行时，根据组件配置决定：
    - 是否先加载 `neirong`
    - 请求哪张业务表
    - 应用哪种排序和过滤方式

- 通用 / 自定义数据表适配
  - 定义「通用数据」对应的标准表结构与别名映射（商品表、文章表、知识库表等）
  - 定义「自定义数据」的约定访问方式（如通过表别名、控件 ID 组合访问）
  - 在后端封装一层通用的数据访问接口，屏蔽不同表结构差异，对小程序端暴露统一数据结构

- 组件级数据驱动渲染
  - 为商品列表 / 内容列表等组件增加基于接口返回数据的渲染模板（支持图片、标题、副标题、价格等）
  - 支持组件根据数据来源类型（通用 / 自定义）加载不同字段映射配置
  - 支持在装修端配置列表卡片样式（展示字段、顺序、是否显示价格/销量等），并写入配置 JSON

- 调试与监控
  - 在后端增加算法推荐相关接口的详细日志（包括请求条件、排序结果、命中内容 ID 列表）
  - 在小程序端为数据加载过程增加关键步骤日志（加载 `neirong`、加载业务表、最终渲染数量）
  - 设计基础监控指标：推荐命中数、空列表比例、接口耗时等，为后续优化算法提供数据依据

### 明道云集成

#### API调用组件

##### MingdaoYunAddAPI（新增数据）
- **文件路径**：`mingdao/MingdaoAdd.js`
- **接口地址**：`https://api.mingdao.com/v2/open/worksheet/addRow`
- **功能**：向明道云工作表新增一行数据
- **核心方法**：`getData(worksheetId, controls)`
- **参数**：
  - `worksheetId`：工作表ID
  - `controls`：新增字段数组，每个对象包含id和value
- **返回**：`{success, data, error_msg, error_code}`
- **使用场景**：商家保存页面配置、新增商品等

##### MingdaoYunQueryAPI（查询单条数据）
- **文件路径**：`mingdao/MingdaoQuery.js`
- **接口地址**：`https://api.mingdao.com/v2/open/worksheet/getRowByIdPost`
- **功能**：根据rowId查询明道云工作表中的单条数据
- **核心方法**：`getData(rowid, worksheetId)`
- **参数**：
  - `rowid`：行ID（唯一标识符）
  - `worksheetId`：工作表ID
- **返回**：`{success, data, error_msg, error_code}`
- **使用场景**：加载商家配置、获取商品详情等

##### MingdaoYunUpdateAPI（更新数据）
- **文件路径**：`mingdao/MingdaoUpdate.js`
- **接口地址**：`https://api.mingdao.com/v2/open/worksheet/editRow`
- **功能**：更新明道云工作表中的数据
- **核心方法**：`getData(rowid, worksheetId, controls)`
- **参数**：
  - `rowid`：行ID
  - `worksheetId`：工作表ID
  - `controls`：更新字段数组，每个对象包含controlId和value
- **返回**：`{success, data, error_msg, error_code}`
- **使用场景**：更新页面配置、修改商品信息等

##### MingdaoYunArrayAPI（批量查询数据）
- **文件路径**：`mingdao/MingdaoArray.js`
- **接口地址**：`https://api.mingdao.com/v2/open/worksheet/getFilterRows`
- **功能**：根据条件批量查询明道云工作表中的数据
- **核心方法**：`getData(params)`
- **参数**：
  - `worksheetId`：工作表ID
  - `viewId`：视图ID（可选）
  - `pageSize`：每页数量（默认50）
  - `pageIndex`：页码（默认1）
  - `keyWords`：关键词搜索（可选）
  - `listType`：列表类型（默认0）
  - `controls`：查询字段（可选）
  - `filters`：筛选条件数组（可选）
  - `sortId`：排序字段ID（可选）
  - `isAsc`：是否升序（默认false）
- **返回**：`{success, data, error_msg, error_code}`
- **使用场景**：获取商品列表、订单列表等

##### 组件使用示例

```javascript
// 新增数据
const addAPI = new MingDaoYunAddAPI();
const result = await addAPI.getData('worksheetId', [
  {id: 'controlId1', value: 'value1'},
  {id: 'controlId2', value: 'value2'}
]);

// 查询单条数据
const queryAPI = new MingDaoYunQueryAPI();
const result = await queryAPI.getData('rowid', 'worksheetId');

// 更新数据
const updateAPI = new MingDaoYunUpdateAPI();
const result = await updateAPI.getData('rowid', 'worksheetId', [
  {controlId: 'controlId1', value: 'newValue1'}
]);

// 批量查询数据
const arrayAPI = new MingDaoYunArrayAPI();
const result = await arrayAPI.getData({
  worksheetId: 'worksheetId',
  pageSize: 20,
  pageIndex: 1,
  filters: [
    {controlId: 'controlId1', dataType: 2, value: ['value1']}
  ]
});
```

##### 组件特性

- **统一接口格式**：所有组件返回 `{success, data, error_msg, error_code}`
- **自动重试**：网络错误时自动重试1次
- **超时控制**：5秒超时自动取消请求
- **详细日志**：完整的请求和响应日志输出
- **错误处理**：统一的异常捕获和错误返回



#### 认证方式
- 使用商户密钥（appKey）和签名（sign）
- 通过URL参数传递商家ID（mRowid）

#### 数据存储
- 商家配置存储在明道云工作表中
- 页面配置以JSON格式存储
- 支持多版本管理

#### 登录逻辑
1. 用户访问商家后台
2. URL参数携带mRowid
3. 调用明道云API验证商家身份
4. 加载商家配置到localStorage
5. 显示商家专属数据

#### 明道云数据结构

```json
{
  "商家ID": "123456",
  "配置名称": "首页配置",
  "全局配置": {
    "主题色": "#667eea",
    "导航栏": {
      "背景色": "#000000",
      "文字色": "#ffffff"
    }
  },
  "底部导航栏": {
    "背景色": "#ffffff",
    "选中色": "#667eea",
    "未选中色": "#999999",
    "列表": [
      {
        "名称": "首页",
        "选中图标": "home-active.png",
        "未选中图标": "home.png",
        "页面ID": "page-001"
      }
    ]
  },
  "页面配置": [
    {
      "页面ID": "page-001",
      "页面名称": "首页",
      "组件列表": [
        {
          "组件名称": "轮播图",
          "组件ID": "comp-001",
          "组件项": ["image1.png", "image2.png", "image3.png"],
          "属性": {
            "高度": 200,
            "自动播放": true,
            "间隔": 3000
          }
        }
      ]
    }
  ]
}
```

App({
  globalData: {
    merchantId: '123456'
  },

  onLaunch(options) {
    console.log('小程序启动');
    console.log('商家ID:', this.globalData.merchantId);  // 这里会输出商家ID
  }
});App({
  globalData: {
    merchantId: '123456'
  },

  onLaunch(options) {
    console.log('小程序启动');
    console.log('商家ID:', this.globalData.merchantId);  // 这里会输出商家ID
  }
});### 商家ID传递机制

#### URL参数传递
- 商家后台URL：`https://100000whys.cn/project/fsj/merchantDashboard/decoration.html?mRowid={商家ID}`
- mRowid参数：明道云中的商家唯一标识符
- 示例：`?mRowid=123456`

#### 商家ID存储流程
1. **前端获取商家ID**
   ```javascript
   const urlParams = new URLSearchParams(window.location.search);
   const merchantId = urlParams.get('mRowid');
   ```

2. **存储到全局变量**
   ```javascript
   window.merchantId = merchantId;
   localStorage.setItem('merchantId', merchantId);
   ```

3. **打包时传递给后端**
   ```javascript
   const config = {
     merchantId: merchantId,
     globalConfig: globalConfig,
     tabBarConfig: tabBarConfig,
     pages: pages
   };
   ```

#### 小程序全局数据存储

##### app.js中存储商家ID
```javascript
App({
  globalData: {
    merchantId: '' // 商家ID
  },

  onLaunch(options) {
    // 从app.json或配置中获取商家ID
    const merchantId = this.globalData.merchantId;
    console.log('商家ID:', merchantId);
  }
});
```

##### 页面中获取商家ID
```javascript
const app = getApp();
Page({
  onLoad(options) {
    const merchantId = app.globalData.merchantId;
    console.log('当前商家ID:', merchantId);
    
    // 使用商家ID调用相关数据
    this.loadMerchantData(merchantId);
  },

  loadMerchantData(merchantId) {
    // 调用API获取该商家的数据
    wx.request({
      url: 'https://api.example.com/merchant/data',
      data: {
        merchantId: merchantId
      },
      success: (res) => {
        console.log('商家数据:', res.data);
        this.setData({
          merchantData: res.data
        });
      }
    });
  }
});
```

#### 后端生成代码时嵌入商家ID

##### 修改generatePageJS函数
```javascript
function generatePageJS(page, merchantId) {
    const components = page.components || [];
    const data = components.map(comp => {
        const items = comp.componentItems || [];
        const props = comp.properties || {};
        return `    ${comp.componentName}: ${JSON.stringify({ items, ...props })}`;
    }).join(',\n');

    return `Page({
  data: {
${data}
  },

  onLoad(options) {
    console.log('页面加载:', options);
    // 获取商家ID
    const app = getApp();
    const merchantId = app.globalData.merchantId || '${merchantId}';
    console.log('商家ID:', merchantId);
    
    // 加载商家数据
    this.loadMerchantData(merchantId);
  },

  loadMerchantData(merchantId) {
    wx.request({
      url: 'https://api.example.com/merchant/data',
      data: {
        merchantId: merchantId
      },
      success: (res) => {
        this.setData({
          merchantData: res.data
        });
      }
    });
  }
});`;
}
```

##### 修改generateAppJson函数
```javascript
async function generateAppJson(config, outputDir) {
    const appJson = {
        pages: config.pages.map(p => `pages/${p.pageId}/index`),
        window: {
            backgroundTextStyle: 'light',
            navigationBarBackgroundColor: config.globalConfig.navigationBar.backgroundColor,
            navigationBarTitleText: '小程序',
            navigationBarTextStyle: config.globalConfig.navigationBar.textColor === '#ffffff' ? 'white' : 'black'
        },
        globalStyle: {
            navigationBarTextStyle: config.globalConfig.navigationBar.textColor === '#ffffff' ? 'white' : 'black',
            navigationBarTitleText: '小程序',
            navigationBarBackgroundColor: config.globalConfig.navigationBar.backgroundColor
        },
        sitemapLocation: 'sitemap.json'
    };

    if (config.tabBarConfig.list.length > 0) {
        appJson.tabBar = {
            color: config.tabBarConfig.unselectedColor || '#999999',
            selectedColor: config.tabBarConfig.selectedColor || config.globalConfig.themeColor || '#667eea',
            backgroundColor: config.tabBarConfig.backgroundColor || '#ffffff',
            borderStyle: config.tabBarConfig.borderStyle || 'black',
            list: config.tabBarConfig.list.map(tab => ({
                pagePath: `pages/${tab.pageId}/index`,
                text: tab.name,
                iconPath: `images/${tab.unselectedIcon}`,
                selectedIconPath: `images/${tab.selectedIcon}`
            }))
        };
        console.log('生成tabBar配置');
    }

    fs.writeFileSync(path.join(outputDir, 'app.json'), JSON.stringify(appJson, null, 2));
    console.log('生成app.json');
}
```

**配置说明**：
- **window**：所有页面的默认导航栏配置
- **globalStyle**：小程序全局样式（微信基础库2.9.0+支持）
  - 优先级高于window配置
  - 主题色通过 `globalConfig.themeColor` 传递
- **tabBar**：底部导航栏配置
  - 选中色优先使用 `globalConfig.themeColor`
  - 未选中色使用 `tabBarConfig.unselectedColor`
```

##### 生成app.js时嵌入商家ID
```javascript
function generateAppJs(merchantId) {
    return `App({
  globalData: {
    merchantId: '${merchantId}'
  },

  onLaunch(options) {
    console.log('小程序启动');
    console.log('商家ID:', this.globalData.merchantId);
  },

  onShow(options) {
    console.log('小程序显示');
  },

  onHide() {
    console.log('小程序隐藏');
  }
});`;
}
```

#### API调用示例

##### 小程序中调用商家数据
```javascript
const app = getApp();

// 获取商家商品列表
wx.request({
  url: 'https://api.example.com/products',
  data: {
    merchantId: app.globalData.merchantId
  },
  success: (res) => {
    this.setData({
      products: res.data
    });
  }
});

// 获取商家订单列表
wx.request({
  url: 'https://api.example.com/orders',
  data: {
    merchantId: app.globalData.merchantId
  },
  success: (res) => {
    this.setData({
      orders: res.data
    });
  }
});
```

### 开发规范

#### 前端规范
- 不能有光标闪烁和选中字体
- 使用CSS变量管理主题色
- 响应式设计，支持移动端
- 代码简洁，功能相似代码尽可能复用

#### 后端规范
- 统一错误处理
- 接口返回格式统一
- 日志记录完整
- 安全性：不暴露敏感信息

#### 数据规范
- 图片统一使用：JSON.parse(localStorage.getItem('merchantData')).logo[0].large_thumbnail_full_path
- JSON字段使用驼峰命名
- 日期格式：YYYY-MM-DD HH:mm:ss

### 最小可行性验证（MVP）

#### 第一阶段：底部导航栏样式
1. 设计两种风格的底部导航栏
2. 在页面设计器中预览效果
3. 生成对应的JSON配置

#### 第二阶段：基础组件
1. 实现轮播图组件
2. 实现功能列表组件
3. 实现图片组件
4. 实现文本组件

#### 第三阶段：代码生成
1. 实现后端代码生成API
2. 实现ZIP打包功能
3. 测试下载和导入流程

### 技术架构图

```
┌─────────────────────────────────────────────────────────┐
│                    商家端后台（merchantDashboard）         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐ │
│  │ 首页     │  │店铺管理  │  │门店装修  │  │其他模块│ │
│  └──────────┘  └──────────┘  └──────────┘  └────────┘ │
│                        │                                │
│                        ▼                                │
│              ┌──────────────────┐                       │
│              │  明道云API调用    │                       │
│              └──────────────────┘                       │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    后端服务（宝塔部署）                   │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Node.js + Express                               │   │
│  │  - 代码生成引擎                                  │   │
│  │  - ZIP打包                                      │   │
│  │  - 配置管理                                     │   │
│  └──────────────────────────────────────────────────┘   │
│                        │                                │
│                        ▼                                │
│              ┌──────────────────┐                       │
│              │  复制wxApp基础代码 │                       │
│              └──────────────────┘                       │
│                        │                                │
│                        ▼                                │
│              ┌──────────────────┐                       │
│              │  根据配置生成页面  │                       │
│              └──────────────────┘                       │
│                        │                                │
│                        ▼                                │
│              ┌──────────────────┐                       │
│              │  生成app.json     │                       │
│              └──────────────────┘                       │
│                        │                                │
│                        ▼                                │
│              ┌──────────────────┐                       │
│              │  打包成ZIP        │                       │
│              └──────────────────┘                       │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    原生小程序（wxApp）                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │ 首页     │  │分类      │  │其他页面  │             │
│  └──────────┘  └──────────┘  └──────────┘             │
│                    │                                    │
│                    ▼                                    │
│              ┌──────────┐                              │
│              │底部导航栏│                              │
│              └──────────┘                              │
└─────────────────────────────────────────────────────────┘
```

### 小程序打包与明道云调用实现方案（实战版）

#### 一、商家ID获取与传递（a# 前缀处理）
- 明道云传入参数：`mRowid=a#698826f3b35652a8d4f60e21`
- 装修页 `decoration.html` 中通过 JS 解析 URL：
  - 提取 `mRowid` 参数；
  - 去掉 `a#` 前缀，仅保留真实商家ID：`698826f3b35652a8d4f60e21`
- 存储策略：
  - `window.merchantId = parsedId`
  - `localStorage.setItem('merchantId', parsedId)`
- 打包时，从 `localStorage`/全局变量中取出“去掉 a# 的商家ID”，写入打包请求 JSON：
  ```javascript
  const config = {
    merchantId: parsedMerchantId, // 无 a# 前缀
    globalConfig,
    tabBarConfig,
    pages
  };
  ```

#### 二、后端打包服务（Node + Express）
- 运行目录：`/www/wwwroot/100000whys.cn/project/fsj/merchantDashboard`
- 技术栈：
  - `Node.js + Express` 提供 HTTP API 和健康检查；
  - `archiver` 打包小程序 ZIP；
  - `fs.cpSync` 复制基础 `wxApp` 框架代码。
- 关键常量：
  - 监听端口：`PORT = 3001`
  - 基础框架目录：`wxApp/`
  - 输出目录：`output/`
- 生成流程：
  1. `POST /api/generate-miniprogram` 接收前端配置（含 `merchantId`）；
  2. 调用 `copyBaseFramework(outputDir)` 复制 `wxApp` 的基础代码（app.wxss、utils、images、components 等）；
  3. 调用 `generateAppJs(merchantId, outputDir)` 生成带商家ID及初始化逻辑的 `app.js`；
  4. 遍历配置中的 `pages` 调用 `generatePage(...)` 生成每个页面的 `index.js / index.wxml / index.wxss / index.json`；
  5. 调用 `generateAppJson(config, outputDir)` 生成 `app.json`；
  6. 使用 `createZip(sourceDir, zipPath)` 打包为 ZIP，返回下载地址 `/download/miniprogram_xxx.zip`。

#### 二、商家实际rowid获取流程

在首页加载时，通过 URL 参数带来的 `mRowid`（初始商家标识）查询 `shangjia` 表，获取该商家在明道云中的实际 `rowid`，然后赋值给全局变量供后续组件数据加载使用。

**1. app.js 全局变量定义**

在 `wxApp/app.js` 中定义 `mRowid` 全局变量：

```javascript
globalData: {
  userInfo: null,
  merchantId: '{商家ID}',
  mRowid: ''
}
```

**2. 首页 onLoad 中调用初始化方法**

在生成的页面 JS 中，在 `onLoad` 生命周期中先调用 `initShangjiaRowid` 方法获取商家实际 rowid：

```javascript
async onLoad(options) {
  console.log('${page.pageName}页面加载', options);
  const app = getApp();
  const appMerchantId = app && app.globalData && app.globalData.merchantId ? app.globalData.merchantId : '${merchantId || ''}';
  console.log('商家ID:', appMerchantId);
  console.log('小程序版本: ${MINIPROGRAM_VERSION}');
  
  await this.initShangjiaRowid(appMerchantId);
  
  console.log('=== 开始加载组件数据 ===');
  // 加载组件数据...
  console.log('=== 组件数据加载结束 ===');
},
```

**3. initShangjiaRowid 方法实现**

通过 `mRowid` 查询 `shangjia` 表获取商家实际 rowid：

```javascript
async initShangjiaRowid(mRowid) {
  try {
    const api = require('../../utils/MingdaoYunArrayAPI');
    const apiInstance = new api();
    
    const filters = [
      {
        'controlId': 'mRowid',
        'dataType': 2,
        'spliceType': 1,
        'filterType': 2,
        'value': mRowid
      }
    ];
    
    const result = await apiInstance.getData({
      worksheetId: 'shangjia',
      filters: filters,
      pageSize: 1,
      pageIndex: 1
    });
    
    console.log('=== 查询shangjia表返回结果 ===');
    console.log(JSON.stringify(result, null, 2));
    
    if (result.success && result.data && result.data.rows && result.data.rows.length > 0) {
      const shangjiaRowid = result.data.rows[0].rowid;
      console.log('商家rowid:', shangjiaRowid);
      app.globalData.mRowid = shangjiaRowid;
    } else {
      console.log('未找到对应的商家记录');
    }
  } catch (error) {
    console.error('查询shangjia表失败:', error);
  }
},
```

**4. 过滤条件说明**

查询 `shangjia` 表时的过滤条件：

| 字段       | 值       | 说明                   |
| ---------- | -------- | ---------------------- |
| controlId  | `mRowid` | 明道云表中的字段名     |
| dataType   | `2`      | 数值类型               |
| spliceType | `1`      | AND 条件               |
| filterType | `2`      | 等于                   |
| value      | `mRowid` | URL 参数带来的商家标识 |

**5. 数据流**

```
URL参数 ?mRowid=xxx 
  → app.globalData.merchantId 
  → initShangjiaRowid() 查询 shangjia 表 
  → 获取商家实际 rowid 
  → app.globalData.mRowid 
  → 供后续组件数据加载使用（如轮播图、功能列表等）
```

#### 三、健康检查与 502 排查

##### 1. 健康检查接口
- 后端 `server.js` 中提供：
  ```javascript
  app.get('/health', (req, res) => {
    res.json({
      status: 'ok',
      timestamp: new Date().toISOString(),
      uptime: process.uptime()
    });
  });
  ```
- Nginx 将 `https://100000whys.cn/health` 反向代理到 `http://127.0.0.1:3001/health`；
- 若 `/health` 返回 200 且 `status: "ok"`，说明：
  - Node 服务在线；
  - 端口监听正常；
  - Nginx → Node 链路畅通。

##### 2. 常见 502 原因及解决
- 场景一：pm2 进程挂掉（如 `Cannot find module 'express'`）
  - 表现：`/health` 返回 Nginx 502 页面；
  - 排查步骤：
    1. `pm2 status` 查看 `miniprog` / `miniprogram-generator` 状态；
    2. `pm2 logs miniprog --lines 80` 查看错误；
    3. 若报 `Cannot find module 'express'`：
       ```bash
       cd /www/wwwroot/100000whys.cn/project/fsj/merchantDashboard
       npm install --production
       # 或至少 npm install express
       pm2 restart miniprog
       ```
    4. 本机测试：`curl http://127.0.0.1:3001/health`，确认返回 JSON。
- 场景二：端口被其他进程占用（`EADDRINUSE: address already in use :::3001`）
  - 表现：日志中有端口占用错误；
  - 解决：
    - 先通过 `pm2 status` 找到冲突的旧服务（如早期的 `miniprog`）；
    - `pm2 delete 旧进程名` 释放端口；
    - 再 `pm2 restart miniprogram-generator`。

##### 3. 运维建议
- 依赖安装：
  - 每次更新 `server.js` 或部署新环境后，务必在对应目录执行 `npm install --production`；
  - 避免在错误目录安装依赖（以误导 pm2）。
- 监控建议：
  - 重要操作后手动访问 `/health`，确认状态为 200；
  - 看到 502 时优先检查 pm2 状态和错误日志文件。

### 开发优先级

#### P0（必须）
1. 底部导航栏两种样式设计
2. 页面树结构实现
3. 基础组件拖拉拽
4. JSON配置生成

#### P1（重要）
1. 后端代码生成API
2. ZIP打包下载
3. 明道云数据存储

#### P2（优化）
1. 组件属性编辑
2. 实时预览
3. 版本管理
4. 模板功能

#### P3（架构优化）
1. 组件模块化架构
2. 组件注册机制
3. 组件独立文件管理
